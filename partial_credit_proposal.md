# 부분 점수 산정 정책(Partial Credit Policy) 개선 제안서

본 문서는 교수님의 채점 지침에 따라 문항 배점별로 유연한 부분 점수 설정을 지원하기 위한 기능 개선안을 정리합니다.

## 1. 부분 점수 산정 모델 (Proposed Models)

| 모델명 | 산정 공식 | 특징 및 장점 |
| :--- | :--- | :--- |
| **정답 비율 비례 (Proportional)** | `(맞은 개수 / 정답 항목 수) × 배점` | 가장 표준적인 방식. 배점에 관계없이 자동 스케일링됨. |
| **항목당 고정 점수 (Fixed Points)** | `MIN(맞은 개수 × 항목당 점수, 배점)` | "하나당 1점"과 같이 명시적인 지침이 있을 때 적용 용이. |
| **감점 방식 (Deduction)** | `MAX(배점 - (누락/오답 × 감점액), 0)` | 다 맞는 것을 기본으로 하되 실수에 대해 감점하는 엄격한 기준. |
| **임계값 방식 (Threshold)** | `IF(맞은 개수 >= N, 계산 시작, 0)` | 일정 개수 이상을 맞춰야 점수를 부여하여 변별력 강화. |

## 2. UI/UX 구현 계획 (SettingsPanel)

조교들이 직관적으로 정책을 설정할 수 있도록 다음과 같은 인터페이스를 추가합니다.

- **정책 선택 드롭다운**: 위 4가지 모델 중 하나를 선택.
- **세부 수치 입력 필드**: 
    - (고정 점수) '항목당 점수' 입력.
    - (임계값) '최소 정답 개수' 입력.
- **소수점 처리 옵션**: 반올림 / 올림 / 버림 / 0.5점 단위 처리 등 선택.
- **오답 처리 정책**: '오답 포함 시 0점' vs '오답 제외 후 정답만 계산' 토글 제공.

## 3. 기술적 구현 방안

- **`preGradeService.js` 확장**: `gradeTypeC` 함수에 `options` 객체를 통해 정책 파라미터를 전달하고, 선택된 모델에 따라 점수를 계산하는 Logic Switch 구현.
- **`App.jsx` 상태 관리**: `DEFAULT_OPTIONS`에 정책 관련 필드(`partialPolicy`, `pointsPerItem`, `roundingMode` 등) 추가 및 로컬 스토리지 저장 연동.

## 4. 기대 효과

1. **교수님 지침 완벽 수용**: 복잡한 부분 점수 부여 기준을 알고리즘으로 자동화.
2. **채점 일관성 확보**: 조교마다 다를 수 있는 부분 점수 계산의 실수 방지.
3. **작업 효율성**: 배점이 다른 여러 문항을 한 번의 설정으로 일괄 채점 가능.

---
*작성일: 2026-05-01*
*작성자: Antigravity AI Assistant*
