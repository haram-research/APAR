# APAR 정답키(D열) 작성 가이드

> APAR v1.2.0 채점 엔진 기준  
> 작성: 2026-05-22

이 문서는 APAR이 D열(정답키)을 파싱하는 방식을 설명하고,  
채점이 의도대로 동작하도록 D열을 작성하는 방법을 안내합니다.

---

## 기본 형식

D열의 정답키는 **하나 이상의 `[...]` 블록**으로 구성됩니다.

```
[허용 표현 1][허용 표현 2][...]
```

- 각 `[...]` 블록은 **서로 다른 허용 표현**(대소문자 구분 없음)입니다.
- 학생이 어느 하나와 일치하면 정답 처리합니다.

### 예시

```
[genetically determined specialization]
```
→ "genetically determined specialization" 하나만 정답

```
[genetically determined specialization][GDS]
```
→ "genetically determined specialization" 또는 "GDS" 정답

---

## 문제 유형별 작성 방법

APAR은 D열을 보고 문제 유형을 **자동으로** 감지합니다.  
유형별 작성 방법을 지키면 의도한 채점 방식이 적용됩니다.

---

### Type A — 단답/구 정답형

**감지 조건**: 쉼표 구분 항목 4개 이하, 단어 수 4개 이하

**권장 형식**:

```
[정답 표현]
```

**특징**:
- 대소문자·괄호·공백 차이는 자동 무시 (예: `(genetically)(determined)(specialization)` → 정답 처리)
- 오탈자 허용은 기본 비활성. 활성화 시 **편집거리 1만 인정**, 부분점수 = `round(배점÷3)` 적용 권장
- 편집거리 ≥ 2는 0점(검토 권장)으로 처리

**올바른 예시**:
```
[genetically determined specialization]
[Paradox of Language Acquisition]
[nativist hypothesis]
```

**피해야 할 형식**:
```
[genetically, determined, specialization]   ← 쉼표를 넣으면 Type C로 오검출
```

---

### Type C — 단어 리스트형 (부분점수 O)

**감지 조건**: 쉼표로 구분된 항목이 **5개 이상**인 form이 하나라도 있으면 Type C

**권장 형식**:

```
[항목1, 항목2, 항목3, 항목4, 항목5, ...]
```

**특징**:
- 쉼표로 구분된 각 항목을 개별적으로 채점
- **순서 무관** 매칭 (학생이 순서를 바꿔 써도 정답)
- 부분점수 지원: 기본값 = `(맞은 개수 / 전체) × 배점`, 반올림
- 오탈자(편집거리 1) 허용 기본 활성 (strict 모드: 4–7자 단어까지)
- 중복 항목 작성 시 두 번째 이후는 자동 무시(오답 처리 안 됨)

**복수 허용 표현이 있는 항목** (같은 위치에 여러 대안 허용):

```
[brillig, slithy, toves, gyre, gimble, wabe, jaws, time, son, foe]
[brillig, slithy, toves, gyre, gimble, wabe, claws, time, son, foe]
```
→ 7번째 위치에서 "jaws" 또는 "claws" 모두 정답

**선택적 추가 항목** (없어도 감점 없음):

항목 수가 다른 form을 나란히 작성하면, 짧은 쪽의 항목 수가 기준 점수로 사용됩니다.

```
['Twas, brillig, slithy, toves, gyre, gimble, wabe, jaws, time, son, foe]
[brillig, slithy, toves, gyre, gimble, wabe, jaws, time, son, foe]
```
→ 총 10항목이 기준. `'Twas`를 쓰면 보너스(감점 없음)

**주의**: 항목이 4개 이하이면 Type A로 분류됩니다.  
Type C로 처리하려면 **반드시 5개 이상** 나열하세요.

---

### Type D — 번호 선택형

**감지 조건**: 모든 form이 숫자·공백·쉼표·괄호로만 구성

**권장 형식**:

```
[1, 3, 5]
[①, ③, ⑤]
```

**특징**:
- 숫자 집합의 완전 일치만 정답 (기본: 순서 무관)
- "1, 3, 5" / "5, 3, 1" / "①③⑤" 모두 정답 처리

---

### Type E — 문장 서술형

**감지 조건**: 단어 수가 5개 이상인 form이 있으면 Type E

**권장 형식**:

```
[언어는 인간에게 고유한 능력이다]
```

**특징**:
- 문장 전체를 Levenshtein 유사도로 비교
- 기본 임계값 30% 이하 편집거리: 부분점수 + 검토 권장 표시
- 복잡한 서술형 채점은 LLM 보조 사용 권장

---

## D열 작성 시 주의사항

### 1. 대괄호 `[` `]`를 답안 내용에 포함해야 할 때

답안 자체에 괄호가 들어가야 한다면 **이중 괄호** `[[...]]`로 감쌉니다.

```
[[p], [d]]
```
→ 학생이 "[p], [d]" 형식으로 써야 정답 처리

```
[[n]]
```
→ 학생이 "[n]"을 써야 정답 처리

### 2. 허용 표현이 여러 개일 때

`[...]` 블록을 **이어서** 작성합니다 (쉼표나 공백 없이).

```
[deep structure][심층구조][deep-structure]
```

### 3. Type A와 Type C 경계

| 항목 수 | 유형 | 순서 | 부분점수 |
|--------|------|------|---------|
| 1–4개  | Type A | 강제 (완전 일치) | X |
| 5개 이상 | Type C | 무관 | O |

**3–4단어짜리 고정 구(fixed phrase)는 쉼표 없이** 작성하세요.

```
[genetically determined specialization]   ← 권장 (Type A)
[genetically, determined, specialization] ← 비권장 (3항목 → 단어수 부족으로 여전히 A지만 혼동 유발)
```

### 4. 오탈자 허용 범위 요약

| 단어 길이 | Strict 모드 | Lenient 모드 |
|----------|------------|-------------|
| 1–3자    | 완전 일치만  | 편집거리 1    |
| 4–7자    | 편집거리 1  | 편집거리 2    |
| 8자 이상  | 편집거리 2  | 편집거리 3    |

- **Type A**에서는 오탈자 허용 시 **편집거리 1까지만** 부분점수 인정  
  (편집거리 ≥ 2는 0점 처리 + 조교 검토 권장)
- Type A 오탈자 부분점수 권장값: `round(배점÷3)`  
  예) 3점 → 1점, 5점 → 2점, 7점 → 2점

---

## 체크리스트

문제 출제 후 D열 작성 전에 확인하세요.

- [ ] 단답/구 정답은 `[답안]` 형식으로 쉼표 없이 작성했는가?
- [ ] 리스트 정답은 항목이 5개 이상인가?
- [ ] 동의어·허용 표현을 `[표현1][표현2]` 형식으로 추가했는가?
- [ ] 정답에 대괄호가 포함돼야 하면 이중 괄호 `[[...]]`로 처리했는가?
- [ ] 선택적 항목(감점 없는 보너스)은 항목 수가 적은 form으로 별도 작성했는가?
