# APAR Changelog

## v1.2.0 (2026-05-22)

### 버그 수정

#### P1 — D열 중첩 괄호 파싱 오류 수정
- **증상**: `[[p],[d]]`와 같이 D열에 중첩 괄호가 있으면 정규식이 내부의 `[p]`, `[d]`를 개별 정답으로 추출, 학생이 단순히 "P"만 써도 만점 처리되는 문제 (Q111 해당).
- **수정**: 정규식 `/\[([^\[\]]*)\]/g` → 깊이 추적(depth-tracking) 파서로 교체. 최상위 `[...]` 블록만 추출하므로 `[[p],[d]]` → 하나의 form `[p],[d]`로 정확히 해석.

#### P2 — 문제 유형 자동 감지 임계값 상향 (Type C 오검출 수정)
- **증상**: 쉼표 구분 항목이 3개 이상이면 Type C(리스트)로 분류하던 기준이 너무 낮아, "genetically determined specialization"(Q102), "Paradox Language Acquisition"(Q104) 같은 3단어 구/제목이 Type C로 오검출. 순서 강제가 필요한 답안이 순서 무관하게 채점됨.
- **수정**: Type C 판정 기준을 항목 **5개 이상**으로 상향 (`listMinItems` 옵션, 기본값 5). Q102, Q104는 Type A로 올바르게 분류.

#### P3 — 4자 단어 오탈자 허용 범위 수정
- **증상**: Levenshtein 엄격 모드에서 4자 이하 단어에 편집거리 0만 허용, `wabe`, `gyre`, `jaws`, `bite`, `Shun` 등 4자 단어 오탈자를 모두 오답 처리 (Q108, Q110 해당).
- **수정**: 엄격 모드 기준 `≤4자 → 0` → `≤3자 → 0` 변경. 4–7자 단어는 편집거리 1 이하 허용.

#### P4 — Type C 중복 토큰 오답 처리 수정
- **증상**: 학생이 같은 단어를 실수로 두 번 쓴 경우(`frumious, frumious, long, manxome`), 두 번째 토큰이 이미 사용된 위치에 재매칭되지 못해 '오답'으로 처리 → `wrongPolicy:zero`로 전체 0점 (Q109 해당).
- **수정**: 이미 매칭된 토큰의 재출현을 `duplicate`으로 감지하고 오답/누락 모두에서 제외. reason에 "중복 무시(xxx)" 표시.

#### P5 — Type C 가변 길이 허용 form 기준 항목 수 수정
- **증상**: 허용 form 중 11항목짜리와 10항목짜리가 혼재할 때 `totalItems=11`로 산정, 10개를 정확히 쓴 학생도 10/11 점만 받는 문제 (Q108 해당).
- **수정**: 필수 항목 수(`totalItems`)를 **가장 짧은 form의 길이**로 설정. 더 긴 form의 추가 항목은 선택적으로 허용(채워도 되고 안 채워도 감점 없음).

#### P6 — Type C 부분점수 '실패 임계값' 정책 추가
- **내용**: 오답 수 + 누락 수가 N개 이상이면 0점, 그 미만이면 비례 점수를 부여하는 `failCutoff` 정책 추가. 기존 '오답 포함 시 전체 0점(`wrongPolicy:zero`)'과 달리 오류 개수 기반 임계치를 설정.
- **UI**: 채점 옵션 → Type C 부분점수 산정 방식에 '실패 임계값' 항목 추가, 기준 개수(N) 입력 필드 제공.

### 개선 사항

#### Type A/C Levenshtein 설정 분리 (P2-C3)
- 기존 `levenshteinMode` 단일 키가 Type A와 Type C에 공유되던 문제 해결.
- `levenshteinModeA` (Type A 전용, 기본: 허용 안 함), `levenshteinModeC` (Type C 전용, 기본: 엄격) 분리.
- **Type A 편집거리 상한 고정**: Type A에서는 편집거리 1까지만 부분점수 인정, 거리 ≥ 2는 0점(검토 권장)으로 처리. 교수님 지정 기준(`round(배점÷3)`)과 일치.
- 채점 옵션 UI에 **33% 부분점수(÷3 반올림)** 프리셋 추가.
- v1.1.0 localStorage 저장값 자동 마이그레이션: `levenshteinMode` → `levenshteinModeC`.

#### 문항 유형 검토 패널 (P2-C2)
- 파일 업로드 후 좌측 패널에 '문항 유형 검토' 섹션이 추가됩니다.
- 각 문항의 자동 감지 유형을 표시하고, 드롭다운으로 A/C/D/E를 강제 지정 가능.
- 강제 지정 내용은 localStorage에 영구 저장. 재채점 버튼으로 변경 사항 반영.

---

## v1.1.0 (2026-05-10)

- 초기 배포
